% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_projects.R
\name{list_projects}
\alias{list_projects}
\title{List the studies behind the datasets}
\usage{
list_projects(project = NULL)
}
\arguments{
\item{project}{Optional project slug or regular expression to filter on.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{project}, \code{title},
\code{repo_url}, \code{visibility}, \code{n_datasets}, and \code{datasets}. \code{repo_url} is \code{NA}
for projects whose code has not been published yet, and \code{visibility} is
\code{"private"} for repositories that only Insper Cidades members can open.
}
\description{
Each dataset comes from a research project with its own processing pipeline.
One project usually produces several datasets, so pipelines are published as
whole repositories rather than as a single script per dataset.
}
\examples{
list_projects()
list_projects("motiva")
}
\seealso{
\code{\link[=browse_project]{browse_project()}} to open a repository, \code{\link[=list_datasets]{list_datasets()}} for the
datasets themselves.
}
