% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_dataverse.R
\name{get_dataverse}
\alias{get_dataverse}
\title{Download any Insper Dataverse dataset from a DOI or URL}
\usage{
get_dataverse(
  x,
  filename = NULL,
  file_pattern = NULL,
  year = NULL,
  files = FALSE
)
}
\arguments{
\item{x}{A DOI (\code{"10.60873/FK2/AOLEOI"}), a DOI URL, or a Dataverse
landing-page URL containing \code{persistentId=doi:...}.}

\item{filename, file_pattern, year}{Optional selectors passed to the same
file-picking logic used by \code{\link[=get_dataset]{get_dataset()}}.}

\item{files}{Logical. If \code{TRUE}, list the deposit's files and return their
names instead of downloading anything. Useful for inspecting an unfamiliar
deposit first.}
}
\value{
A \link[tibble:tibble]{tibble} or \code{sf} object with the \code{"doi"} attribute
set. When \code{files = TRUE}, a character vector of filenames.
}
\description{
Downloads a deposit that has no alias in the package registry. Copy the DOI
or the landing-page URL from Insper's Dataverse and paste it straight into R.
}
\details{
Registered datasets are better served by \code{\link[=get_dataset]{get_dataset()}}, which knows which
file to pick. This function has no such knowledge, so it lists the deposit
and applies the same format preference to whatever it finds. Deposits with an
unusual file layout may need \code{filename} or \code{file_pattern} to resolve, and
formats the package cannot read will raise an error naming the file.

Only DOIs under the \code{10.60873} prefix are served, since the package always
talks to Insper's Dataverse.
}
\examples{
\dontshow{if (live_examples()) withAutoprint(\{ # examplesIf}
# Inspect a deposit before downloading
get_dataverse("10.60873/FK2/YWXLQS", files = TRUE)

# Paste a DOI straight from the Dataverse page
linhas <- get_dataverse("10.60873/FK2/YWXLQS", filename = "dim_line.rds")

# Or the landing-page URL
linhas <- get_dataverse(
  "https://dataverse.datascience.insper.edu.br/dataset.xhtml?persistentId=doi:10.60873/FK2/YWXLQS",
  filename = "dim_line.rds"
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_dataset]{get_dataset()}} for registered datasets.
}
